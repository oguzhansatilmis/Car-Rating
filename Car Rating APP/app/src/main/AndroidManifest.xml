<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.staj2021">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Staj2021">
        <activity android:name=".SkorGoster2Activity"></activity>
        <activity android:name=".SkorGoster" />
        <activity android:name=".AracRatingActivity" />
        <activity android:name=".Sorular1Activity" />
        <activity android:name=".AnketBilgi" />
        <activity android:name=".GirisActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnasayfaActivity" />
        <activity android:name=".KayitolActivity" />
    </application>

</manifest>